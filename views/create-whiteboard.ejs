<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SkillOns</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .create-whiteboard {
            padding: 20px;
            max-width: 800px;
            margin: 0 auto;
        }

        .page-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .page-header h1 {
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        .page-header p {
            color: #666;
            font-size: 16px;
        }

        .form-container {
            background: white;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .form-section {
            margin-bottom: 30px;
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            color: #333;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #f0f0f0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #555;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 6px;
            font-size: 14px;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .collaboration-settings {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-top: 15px;
        }

        .setting-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding: 10px;
            background: white;
            border-radius: 6px;
            border: 1px solid #e0e0e0;
        }

        .setting-item:last-child {
            margin-bottom: 0;
        }

        .setting-info h4 {
            margin: 0 0 5px 0;
            color: #333;
            font-size: 14px;
        }

        .setting-info p {
            margin: 0;
            color: #666;
            font-size: 12px;
        }

        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 50px;
            height: 24px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: .4s;
            border-radius: 24px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 18px;
            width: 18px;
            left: 3px;
            bottom: 3px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background-color: var(--primary-color);
        }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

        .number-input {
            width: 80px;
            padding: 8px;
            border: 1px solid #e0e0e0;
            border-radius: 4px;
            text-align: center;
        }

        .form-actions {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 2px solid #f0f0f0;
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.2s ease;
        }

        .btn-primary {
            background: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            background: #5a6268;
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .help-text {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }

        .error-message {
            color: #dc3545;
            font-size: 14px;
            margin-top: 5px;
        }

        .success-message {
            color: #28a745;
            font-size: 14px;
            margin-top: 5px;
        }

        .preview-card {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-top: 20px;
            border: 2px dashed #dee2e6;
        }

        .preview-card h4 {
            margin: 0 0 15px 0;
            color: #333;
            font-size: 16px;
        }

        .preview-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px;
            background: white;
            border-radius: 4px;
        }

        .preview-item:last-child {
            margin-bottom: 0;
        }

        .preview-label {
            font-weight: 600;
            color: #555;
        }

        .preview-value {
            color: #666;
        }

        @media (max-width: 768px) {
            .create-whiteboard {
                padding: 15px;
            }

            .form-container {
                padding: 20px;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .form-actions {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="create-whiteboard">
        <div class="page-header">
            <h1><i class="fas fa-chalkboard"></i> Create Whiteboard Session</h1>
            <p>Set up a new interactive whiteboard session for your students</p>
        </div>

        <div class="form-container">
            <form id="whiteboardForm" onsubmit="createWhiteboardSession(event)">
                <!-- Basic Information -->
                <div class="form-section">
                    <div class="section-title">
                        <i class="fas fa-info-circle"></i>
                        Basic Information
                    </div>
                    
                    <div class="form-group">
                        <label for="title">Session Title *</label>
                        <input type="text" id="title" name="title" required 
                               placeholder="e.g., Math Lesson - Fractions, Science Lab - Chemistry"
                               maxlength="200">
                        <div class="help-text">Choose a descriptive title that students will easily recognize</div>
                    </div>

                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" 
                                  placeholder="Brief description of what this session will cover..."
                                  maxlength="1000"></textarea>
                        <div class="help-text">Optional: Provide context for students about the session content</div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label for="gradeLevel">Grade Level *</label>
                            <select id="gradeLevel" name="gradeLevel" required>
                                <option value="">Select Grade Level</option>
                                <option value="1st grade">1st Grade</option>
                                <option value="2nd grade">2nd Grade</option>
                                <option value="3rd grade">3rd Grade</option>
                                <option value="4th grade">4th Grade</option>
                                <option value="5th grade">5th Grade</option>
                                <option value="6th grade">6th Grade</option>
                                <option value="7th grade">7th Grade</option>
                                <option value="8th grade">8th Grade</option>
                                <option value="9th grade">9th Grade</option>
                                <option value="10th grade">10th Grade</option>
                                <option value="11th grade">11th Grade</option>
                                <option value="12th grade">12th Grade</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label for="subject">Subject *</label>
                            <select id="subject" name="subject" required>
                                <option value="">Select Subject</option>
                                <option value="English">English</option>
                                <option value="Science">Science</option>
                                <option value="Math">Math</option>
                                <option value="History">History</option>
                                <option value="Geography">Geography</option>
                                <option value="Literature">Literature</option>
                                <option value="Art">Art</option>
                                <option value="Music">Music</option>
                                <option value="Physical Education">Physical Education</option>
                                <option value="Computer Science">Computer Science</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Session Configuration -->
                <div class="form-section">
                    <div class="section-title">
                        <i class="fas fa-cog"></i>
                        Session Configuration
                    </div>

                    <div class="form-group">
                        <label for="sessionMode">Session Mode</label>
                        <select id="sessionMode" name="sessionMode">
                            <option value="live">Live Mode - Real-time teaching with instant updates</option>
                            <option value="recorded">Recorded Mode - Record explanations for later viewing</option>
                        </select>
                        <div class="help-text">Live mode is perfect for real-time classes, while recorded mode is great for creating reusable content</div>
                    </div>

                    <div class="form-group">
                        <label for="tags">Tags</label>
                        <input type="text" id="tags" name="tags" 
                               placeholder="e.g., fractions, algebra, geometry (separate with commas)">
                        <div class="help-text">Add relevant tags to help students find this session later</div>
                    </div>
                </div>

                <!-- Collaboration Settings -->
                <div class="form-section">
                    <div class="section-title">
                        <i class="fas fa-users"></i>
                        Collaboration Settings
                    </div>

                    <div class="collaboration-settings">
                        <div class="setting-item">
                            <div class="setting-info">
                                <h4>Allow Student Writing</h4>
                                <p>Students can add text and equations to the board</p>
                            </div>
                            <label class="toggle-switch">
                                <input type="checkbox" id="studentWritingEnabled">
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="setting-item">
                            <div class="setting-info">
                                <h4>Allow Student Drawing</h4>
                                <p>Students can draw and sketch on the board</p>
                            </div>
                            <label class="toggle-switch">
                                <input type="checkbox" id="studentDrawingEnabled">
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="setting-item">
                            <div class="setting-info">
                                <h4>Maximum Active Students</h4>
                                <p>Limit how many students can actively participate</p>
                            </div>
                            <input type="number" id="maxActiveStudents" class="number-input" 
                                   value="5" min="1" max="20">
                        </div>
                    </div>
                </div>

                <!-- Session Preview -->
                <div class="preview-card" id="sessionPreview" style="display: none;">
                    <h4><i class="fas fa-eye"></i> Session Preview</h4>
                    <div class="preview-item">
                        <span class="preview-label">Title:</span>
                        <span class="preview-value" id="previewTitle">-</span>
                    </div>
                    <div class="preview-item">
                        <span class="preview-label">Grade:</span>
                        <span class="preview-value" id="previewGrade">-</span>
                    </div>
                    <div class="preview-item">
                        <span class="preview-label">Subject:</span>
                        <span class="preview-value" id="previewSubject">-</span>
                    </div>
                    <div class="preview-item">
                        <span class="preview-label">Mode:</span>
                        <span class="preview-value" id="previewMode">-</span>
                    </div>
                    <div class="preview-item">
                        <span class="preview-label">Collaboration:</span>
                        <span class="preview-value" id="previewCollaboration">-</span>
                    </div>
                </div>

                <!-- Form Actions -->
                <div class="form-actions">
                    <button type="button" class="btn btn-secondary" onclick="goBack()">
                        <i class="fas fa-arrow-left"></i> Back
                    </button>
                    <button type="submit" class="btn btn-primary" id="createBtn">
                        <i class="fas fa-plus"></i> Create Session
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Form elements
        const form = document.getElementById('whiteboardForm');
        const titleInput = document.getElementById('title');
        const gradeSelect = document.getElementById('gradeLevel');
        const subjectSelect = document.getElementById('subject');
        const modeSelect = document.getElementById('sessionMode');
        const writingToggle = document.getElementById('studentWritingEnabled');
        const drawingToggle = document.getElementById('studentDrawingEnabled');
        const maxStudentsInput = document.getElementById('maxActiveStudents');
        const createBtn = document.getElementById('createBtn');

        // Preview elements
        const previewCard = document.getElementById('sessionPreview');
        const previewTitle = document.getElementById('previewTitle');
        const previewGrade = document.getElementById('previewGrade');
        const previewSubject = document.getElementById('previewSubject');
        const previewMode = document.getElementById('previewMode');
        const previewCollaboration = document.getElementById('previewCollaboration');

        // Update preview when form changes
        function updatePreview() {
            const title = titleInput.value || '-';
            const grade = gradeSelect.value || '-';
            const subject = subjectSelect.value || '-';
            const mode = modeSelect.value || '-';
            
            const collaboration = [];
            if (writingToggle.checked) collaboration.push('Writing');
            if (drawingToggle.checked) collaboration.push('Drawing');
            if (collaboration.length === 0) collaboration.push('None');

            previewTitle.textContent = title;
            previewGrade.textContent = grade;
            previewSubject.textContent = subject;
            previewMode.textContent = mode;
            previewCollaboration.textContent = collaboration.join(', ');

            // Show preview if we have basic info
            if (title !== '-' && grade !== '-' && subject !== '-') {
                previewCard.style.display = 'block';
            } else {
                previewCard.style.display = 'none';
            }
        }

        // Add event listeners for preview updates
        titleInput.addEventListener('input', updatePreview);
        gradeSelect.addEventListener('change', updatePreview);
        subjectSelect.addEventListener('change', updatePreview);
        modeSelect.addEventListener('change', updatePreview);
        writingToggle.addEventListener('change', updatePreview);
        drawingToggle.addEventListener('change', updatePreview);

        // Create whiteboard session
        async function createWhiteboardSession(event) {
            event.preventDefault();
            
            // Validate required fields
            if (!titleInput.value.trim() || !gradeSelect.value || !subjectSelect.value) {
                alert('Please fill in all required fields');
                return;
            }

            // Disable submit button
            createBtn.disabled = true;
            createBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Creating...';

            try {
                const formData = {
                    title: titleInput.value.trim(),
                    description: document.getElementById('description').value.trim(),
                    gradeLevel: gradeSelect.value,
                    subject: subjectSelect.value,
                    sessionMode: modeSelect.value,
                    collaborationSettings: {
                        studentWritingEnabled: writingToggle.checked,
                        studentDrawingEnabled: drawingToggle.checked,
                        maxActiveStudents: parseInt(maxStudentsInput.value) || 5
                    },
                    tags: document.getElementById('tags').value.split(',').map(tag => tag.trim()).filter(tag => tag)
                };

                const response = await fetch('/api/whiteboard/create', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();

                if (result.success) {
                    // Show success message
                    alert('Whiteboard session created successfully!');
                    
                    // Redirect to the new session
                    window.location.href = `/whiteboard/${result.sessionId}`;
                } else {
                    alert('Error: ' + result.error);
                }
            } catch (error) {
                console.error('Error creating whiteboard session:', error);
                alert('Failed to create whiteboard session. Please try again.');
            } finally {
                // Re-enable submit button
                createBtn.disabled = false;
                createBtn.innerHTML = '<i class="fas fa-plus"></i> Create Session';
            }
        }

        function goBack() {
            window.history.back();
        }

        // Initialize preview
        updatePreview();
    </script>
</body>
</html> 