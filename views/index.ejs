<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillOns - Quiz Platform for Teachers</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        html {
            scroll-behavior: smooth;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); /* Purple gradient background */
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            color: white; /* Default white text */
        }

        /* Navigation Bar Styles */
        .navbar-custom {
            background: rgba(102, 126, 234, 0.95) !important; /* Semi-transparent purple */
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(102, 126, 234, 0.3);
            padding: 1rem 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .navbar-custom .navbar-brand {
            color: white !important; /* White brand text */
            font-size: 1.8rem;
            font-weight: bold;
            text-decoration: none;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }

        .navbar-custom .navbar-brand:hover {
            color: #e8eaf6 !important; /* Light purple on hover */
            transform: scale(1.05);
        }

        .navbar-custom .navbar-nav .nav-link {
            color: white !important; /* White navigation links */
            font-weight: 600;
            margin: 0 0.5rem;
            padding: 0.5rem 1rem !important;
            border-radius: 25px;
            transition: all 0.3s ease;
            text-shadow: 0 1px 2px rgba(0,0,0,0.2);
        }

        .navbar-custom .navbar-nav .nav-link:hover {
            color: #667eea !important;
            background: rgba(255, 255, 255, 0.9);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.2);
            text-shadow: none;
        }

        .navbar-custom .btn-nav {
            margin: 0 0.25rem;
            padding: 0.5rem 1.5rem;
            border-radius: 25px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .btn-nav-primary {
            background: rgba(255, 255, 255, 0.9);
            color: #667eea !important;
            border: 2px solid transparent;
            font-weight: 700;
        }

        .btn-nav-primary:hover {
            background: white;
            color: #5a67d8 !important;
            border: 2px solid white;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
        }

        .btn-nav-outline {
            background: transparent;
            color: white !important;
            border: 2px solid rgba(255, 255, 255, 0.7);
        }

        .btn-nav-outline:hover {
            background: rgba(255, 255, 255, 0.9);
            color: #667eea !important;
            border: 2px solid white;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 255, 255, 0.2);
        }

        .navbar-toggler {
            border: 2px solid rgba(255, 255, 255, 0.5);
            padding: 0.25rem 0.5rem;
            border-radius: 8px;
        }

        .navbar-toggler:focus {
            box-shadow: 0 0 0 0.2rem rgba(255, 255, 255, 0.25);
        }

        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='%23ffffff' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }

        .hero-section {
            padding: 8rem 0 4rem 0; /* Added top padding for fixed navbar */
            text-align: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #5a67d8 100%); /* Purple gradient */
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23ffffff" opacity="0.1"/><circle cx="75" cy="75" r="1" fill="%23ffffff" opacity="0.1"/><circle cx="50" cy="10" r="0.5" fill="%23ffffff" opacity="0.1"/><circle cx="10" cy="60" r="0.5" fill="%23ffffff" opacity="0.1"/><circle cx="90" cy="40" r="0.5" fill="%23ffffff" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
        }

        .hero-section::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 30% 20%, rgba(255,255,255,0.1) 0%, transparent 50%),
                        radial-gradient(circle at 70% 80%, rgba(255,255,255,0.05) 0%, transparent 50%);
            pointer-events: none;
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: bold;
            margin-bottom: 1rem;
            color: white; /* White text for purple background */
            text-shadow: 2px 2px 8px rgba(0,0,0,0.3);
            position: relative;
            z-index: 2;
        }

        .hero-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            color: rgba(255, 255, 255, 0.95); /* Slightly transparent white */
            font-weight: 500;
            position: relative;
            z-index: 2;
            text-shadow: 1px 1px 4px rgba(0,0,0,0.2);
        }

        .lead1 {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1rem;
            margin-bottom: 2rem;
            position: relative;
            z-index: 2;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
        }

        .cta-buttons {
            margin: 2rem 0;
        }

        .cta-button {
            margin: 0.5rem;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            border-radius: 50px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .btn-primary-custom {
            background: rgba(255, 255, 255, 0.95);
            color: #667eea;
            border: 2px solid transparent;
            position: relative;
            z-index: 2;
            font-weight: 700;
        }

        .btn-primary-custom:hover {
            background: white;
            color: #5a67d8;
            border: 2px solid white;
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(255, 255, 255, 0.3);
        }

        .btn-outline-custom {
            background: transparent;
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.8);
            position: relative;
            z-index: 2;
            font-weight: 600;
        }

        .btn-outline-custom:hover {
            background: rgba(255, 255, 255, 0.95);
            color: #667eea;
            border: 2px solid white;
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(255, 255, 255, 0.2);
        }

        .features-section {
            background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
            padding: 4rem 0;
            position: relative;
            color: white;
        }

        .features-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(180deg, rgba(102, 126, 234, 0.8) 0%, rgba(118, 75, 162, 0.9) 50%, rgba(90, 103, 216, 0.8) 100%);
            pointer-events: none;
        }

        .features-section .container {
            position: relative;
            z-index: 1;
        }

        .feature-card {
            text-align: center;
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            margin-bottom: 2rem;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
            color: white;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.3);
        }

        .feature-icon {
            font-size: 3.5rem;
            color: white;
            margin-bottom: 1.5rem;
            display: block;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }

        .debug-info {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 2rem;
            margin: 2rem 0;
            color: white;
        }

        .organization-info {
            background: rgba(255,255,255,0.2);
            border-radius: 10px;
            padding: 1rem;
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-graduation-cap me-2"></i>
                SkillOns
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#features">
                            <i class="fas fa-star me-1"></i>Features
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#pricing">
                            <i class="fas fa-tags me-1"></i>Pricing
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#about">
                            <i class="fas fa-info-circle me-1"></i>About
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">
                            <i class="fas fa-envelope me-1"></i>Contact
                        </a>
                    </li>
                </ul>

                <div class="navbar-nav">
                    <% if (user) { %>
                        <a href="/dashboard" class="btn btn-nav btn-nav-primary">
                            <i class="fas fa-tachometer-alt me-1"></i>Dashboard
                        </a>
                        <a href="/logout" class="btn btn-nav btn-nav-outline">
                            <i class="fas fa-sign-out-alt me-1"></i>Logout
                        </a>
                    <% } else { %>
                        <a href="/login" class="btn btn-nav btn-nav-outline">
                            <i class="fas fa-sign-in-alt me-1"></i>Sign In
                        </a>
                        <a href="/signup" class="btn btn-nav btn-nav-primary">
                            <i class="fas fa-rocket me-1"></i>Get Started
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="hero-section" id="home">
        <div class="container">
            <h1 class="hero-title">
                <i class="fas fa-graduation-cap me-3"></i>
                SkillOns
            </h1>
            <p class="hero-subtitle">
                The Complete SaaS Quiz Platform for Teachers
            </p>
            <p class="lead1">
                Create your own isolated teaching environment with unlimited quizzes,
                multi-language support, and comprehensive student analytics.
            </p>

            <div class="cta-buttons">
                <a href="/signup" class="btn btn-primary-custom cta-button">
                    <i class="fas fa-rocket me-2"></i>
                    Get Started Today
                </a>
                <% if (user) { %>
                <a href="/dashboard" class="btn btn-outline-custom cta-button">
                    <i class="fas fa-tachometer-alt me-2"></i>
                    Go to Dashboard
                </a>
                <% } else { %>
                <a href="/login" class="btn btn-outline-custom cta-button">
                    <i class="fas fa-sign-in-alt me-2"></i>
                    Sign In
                </a>
                <% } %>
            </div>

            <!-- Debug Information (for testing) -->
        <!--    <% if (typeof organization !== 'undefined' && organization) { %>
            <div class="organization-info">
                <h5><i class="fas fa-info-circle me-2"></i>Organization Detected</h5>
                <p><strong>Name:</strong> <%= organization.name %></p>
                <p><strong>Subdomain:</strong> <%= organization.subdomain %></p>
                <p><strong>Plan:</strong> <%= organization.planType %></p>
                <p><strong>URL:</strong> <%= organization.fullDomain %></p>
            </div>
            <% } %>

            <div class="debug-info">
                <h5><i class="fas fa-bug me-2"></i>Debug Information (for testing)</h5>
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>Current Host:</strong> <%= typeof req !== 'undefined' ? (req.get('host') || 'Unknown') : 'Unknown' %></p>
                        <p><strong>User Status:</strong> <%= user ? 'Logged In' : 'Not Logged In' %></p>
                        <% if (user) { %>
                        <p><strong>User Role:</strong> <%= user.role %></p>
                        <p><strong>Display Name:</strong> <%= user.displayName %></p>
                        <% } %>
                    </div>
                    <div class="col-md-6">
                        <p><strong>Organization Context:</strong> <%= typeof organizationId !== 'undefined' && organizationId ? 'Detected' : 'None' %></p>
                        <p><strong>Node Environment:</strong> <%= process.env.NODE_ENV || 'development' %></p>
                        <p><strong>Server Status:</strong> <span class="text-success">Running</span></p>
                    </div>
                </div>
            </div>
        </div>
    </div> -->

    <!-- Features Section -->
    <div class="features-section" id="features">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-5">
                    <h2 style="color: white; font-weight: bold; margin-bottom: 1rem; text-shadow: 2px 2px 4px rgba(0,0,0,0.3);">Why Choose SkillOns?</h2>
                    <p class="lead" style="color: rgba(255, 255, 255, 0.9); font-size: 1.2rem; text-shadow: 1px 1px 2px rgba(0,0,0,0.2);">Complete isolation, unlimited possibilities</p>
                </div>
            </div>

            <div class="row">
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-users feature-icon"></i>
                        <h4>Your Own Environment</h4>
                        <p>Get your personalized subdomain with complete data isolation. Your students, quizzes, and content are completely separate from other teachers.</p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-globe feature-icon"></i>
                        <h4>Multi-Language Support</h4>
                        <p>Create quizzes in English, Spanish, French, and Kannada. Our advanced parsing engine understands different question formats automatically.</p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-chart-line feature-icon"></i>
                        <h4>Comprehensive Analytics</h4>
                        <p>Track student progress, quiz performance, and learning outcomes with detailed analytics and reporting tools.</p>
                    </div>
                </div>
            </div>

            <div class="row mt-4">
                <div class="col-md-6">
                    <div class="feature-card">
                        <i class="fas fa-cloud-upload-alt feature-icon"></i>
                        <h4>Easy Content Management</h4>
                        <p>Upload PDFs, Word documents, and PowerPoint files. Share study materials with grade-level filtering.</p>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="feature-card">
                        <i class="fas fa-shield-alt feature-icon"></i>
                        <h4>Secure & Scalable</h4>
                        <p>Enterprise-grade security with automatic backups, SSL encryption, and scalable infrastructure.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Teachers Directory -->
    <% if (teachers && teachers.length > 0) { %>
    <div class="features-section" style="background: #f8f9fa;">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-4">
                    <h2 style="color: #667eea;">
                        <i class="fas fa-users me-2"></i>
                        Find Your Teacher
                    </h2>
                    <p class="lead text-muted">Connect with your teacher and join their organization using the code below</p>
                </div>
            </div>

            <div class="row">
                <% teachers.forEach(function(teacher) { %>
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 shadow-sm border-0" style="border-radius: 15px; transition: transform 0.3s ease;">
                        <div class="card-body text-center p-4">
                            <div class="mb-3">
                                <div class="teacher-avatar mx-auto mb-3" style="width: 60px; height: 60px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center;">
                                    <i class="fas fa-chalkboard-teacher text-white" style="font-size: 24px;"></i>
                                </div>
                                <h5 class="card-title mb-2" style="color: #2c3e50;"><%= teacher.displayName %></h5>
                            </div>

                            <div class="organization-info mb-3">
                                <h6 class="mb-2" style="color: #667eea;">
                                    <i class="fas fa-school me-1"></i>
                                    <%= teacher.organizationId.name %>
                                </h6>
                                <div class="org-code-display">
                                    <label class="form-label small text-muted mb-1">Organization Code:</label>
                                    <div class="input-group input-group-sm">
                                        <input type="text"
                                               class="form-control text-center fw-bold"
                                               value="<%= teacher.organizationId.subdomain %>"
                                               readonly
                                               id="orgCode_<%= teacher._id %>"
                                               style="font-family: monospace; background: #f8f9fa; border: 2px solid #e9ecef;">
                                        <button class="btn btn-outline-primary btn-sm"
                                                type="button"
                                                onclick="copyCode('<%= teacher._id %>')"
                                                title="Copy organization code">
                                            <i class="fas fa-copy"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>

                            <div class="action-buttons">
                                <a href="/signup?code=<%= teacher.organizationId.subdomain %>"
                                   class="btn btn-primary btn-sm rounded-pill px-3 mb-2">
                                    <i class="fas fa-user-plus me-1"></i>
                                    Join as Student
                                </a>
                                <div class="text-muted small mt-2">
                                    <i class="fas fa-info-circle me-1"></i>
                                    Click to join this teacher's organization
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>

            <div class="row mt-4">
                <div class="col-12 text-center">
                    <div class="alert alert-info d-inline-block" style="border-radius: 15px;">
                        <i class="fas fa-lightbulb me-2"></i>
                        <strong>How to join:</strong> Click "Join as Student" or copy the organization code and use it during manual signup.
                    </div>
                </div>
            </div>
        </div>
    </div>
    <% } %>

    <!-- Testing Instructions -->
  <!--  <div class="hero-section" style="padding: 2rem 0; background: #f8f9fa; color: #2c3e50;">
        <div class="container">
            <div class="debug-info">
                <h5><i class="fas fa-flask me-2"></i>Testing Instructions</h5>
                <div class="row">
                    <div class="col-md-6">
                        <h6>🚀 Getting Started</h6>
                        <ol class="text-start">
                            <li>Click "Start Teaching Today" to create an organization</li>
                            <li>Choose your subdomain (e.g., "myschool")</li>
                            <li>Complete the 3-step setup process</li>
                            <li>Access your org at myschool.localhost:3000</li>
                        </ol>
                    </div>
                    <div class="col-md-6">
                        <h6>🧪 Test Multi-Tenancy</h6>
                        <ol class="text-start">
                            <li>Create multiple organizations with different subdomains</li>
                            <li>Access each via their subdomain</li>
                            <li>Verify complete data isolation</li>
                            <li>Test different subscription plans</li>
                        </ol>
                    </div>
                </div>

                <div class="row mt-3">
                    <div class="col-12">
                        <h6>🔧 Development Testing</h6>
                        <p>Run <code>node test-saas.js</code> to create test organizations and verify the setup.</p>
                        <p><strong>Test Organizations:</strong></p>
                        <ul class="text-start">
                            <li><code>testschool.localhost:3000</code> - Free plan</li>
                            <li><code>demoschool.localhost:3000</code> - Basic plan</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div> -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Copy organization code functionality
        function copyCode(teacherId) {
            const codeInput = document.getElementById('orgCode_' + teacherId);
            const copyBtn = codeInput.nextElementSibling;

            // Select and copy the text
            codeInput.select();
            codeInput.setSelectionRange(0, 99999); // For mobile devices

            try {
                document.execCommand('copy');

                // Visual feedback
                const originalIcon = copyBtn.innerHTML;
                copyBtn.innerHTML = '<i class="fas fa-check"></i>';
                copyBtn.classList.remove('btn-outline-primary');
                copyBtn.classList.add('btn-success');

                // Show success message
                showCopyMessage('Organization code copied to clipboard!', 'success');

                // Reset button after 2 seconds
                setTimeout(() => {
                    copyBtn.innerHTML = originalIcon;
                    copyBtn.classList.remove('btn-success');
                    copyBtn.classList.add('btn-outline-primary');
                }, 2000);

            } catch (err) {
                console.error('Failed to copy: ', err);
                showCopyMessage('Failed to copy code. Please copy manually.', 'error');
            }
        }

        // Show copy message
        function showCopyMessage(message, type) {
            // Remove existing message if any
            const existingMessage = document.getElementById('copyMessage');
            if (existingMessage) {
                existingMessage.remove();
            }

            // Create new message
            const messageDiv = document.createElement('div');
            messageDiv.id = 'copyMessage';
            messageDiv.className = `alert alert-${type === 'success' ? 'success' : 'warning'} alert-dismissible fade show position-fixed`;
            messageDiv.style.cssText = 'top: 20px; right: 20px; z-index: 9999; max-width: 300px;';
            messageDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-triangle'} me-2"></i>
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;

            document.body.appendChild(messageDiv);

            // Auto-remove after 3 seconds
            setTimeout(() => {
                if (messageDiv.parentNode) {
                    messageDiv.remove();
                }
            }, 3000);
        }

        // Add hover effects to teacher cards
        document.addEventListener('DOMContentLoaded', function() {
            const teacherCards = document.querySelectorAll('.card');
            teacherCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-5px)';
                    this.style.boxShadow = '0 10px 25px rgba(0,0,0,0.15)';
                });

                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                    this.style.boxShadow = '';
                });
            });
        });
    </script>

    <!-- Bootstrap JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Smooth scrolling for navigation links -->
    <script>
        // Add smooth scrolling to navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add navbar background on scroll
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar-custom');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(102, 126, 234, 0.98)';
                navbar.style.backdropFilter = 'blur(15px)';
                navbar.style.boxShadow = '0 4px 20px rgba(102, 126, 234, 0.4)';
            } else {
                navbar.style.background = 'rgba(102, 126, 234, 0.95)';
                navbar.style.backdropFilter = 'blur(10px)';
                navbar.style.boxShadow = '0 2px 20px rgba(102, 126, 234, 0.3)';
            }
        });
    </script>
</body>
</html>
