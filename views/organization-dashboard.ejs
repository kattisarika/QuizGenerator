<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= organization.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: <%= organization.branding.primaryColor %>;
            --secondary-color: <%= organization.branding.secondaryColor %>;
        }
        
        .stats-card {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .stats-card:hover {
            transform: translateY(-5px);
        }
        
        .stats-number {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }
        
        .stats-label {
            font-size: 1.1rem;
            opacity: 0.9;
        }
        
        .progress-card {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            margin-bottom: 1.5rem;
        }
        
        .usage-bar {
            background: #f1f5f9;
            border-radius: 10px;
            height: 10px;
            overflow: hidden;
        }
        
        .usage-fill {
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            height: 100%;
            border-radius: 10px;
            transition: width 0.3s ease;
        }
        
        .quick-action {
            background: white;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            padding: 1.5rem;
            text-align: center;
            text-decoration: none;
            color: #1e293b;
            transition: all 0.3s ease;
            display: block;
            margin-bottom: 1rem;
        }
        
        .quick-action:hover {
            border-color: var(--primary-color);
            color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .recent-activity {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
        }
        
        .activity-item {
            padding: 1rem 0;
            border-bottom: 1px solid #f1f5f9;
        }
        
        .activity-item:last-child {
            border-bottom: none;
        }
        
        .org-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
        }
        
        .plan-badge {
            background: rgba(255,255,255,0.2);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            display: inline-block;
        }
    </style>
</head>
<body>
    <!-- Organization Header -->
    <div class="org-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h1 class="mb-2">
                        <i class="fas fa-school me-3"></i>
                        <%= organization.name %>
                    </h1>
                    <p class="mb-1">
                        <i class="fas fa-globe me-2"></i>
                        <%= organization.fullDomain %>
                    </p>
                    <span class="plan-badge">
                        <i class="fas fa-crown me-2"></i>
                        <%= organization.planType.charAt(0).toUpperCase() + organization.planType.slice(1) %> Plan
                    </span>
                </div>
                <div class="col-md-4 text-md-end">
                    <div class="dropdown">
                        <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            <i class="fas fa-cog me-2"></i>Settings
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/organization/settings">
                                <i class="fas fa-cog me-2"></i>Organization Settings
                            </a></li>
                            <li><a class="dropdown-item" href="/organization/members">
                                <i class="fas fa-users me-2"></i>Manage Members
                            </a></li>
                            <li><a class="dropdown-item" href="/organization/billing">
                                <i class="fas fa-credit-card me-2"></i>Billing & Plans
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/teacher/dashboard">
                                <i class="fas fa-chalkboard-teacher me-2"></i>Teacher Dashboard
                            </a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <!-- Statistics Cards -->
            <div class="col-lg-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-number">
                        <%= stats.students %>
                    </div>
                    <div class="stats-label">
                        <i class="fas fa-user-graduate me-2"></i>
                        Students
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-number">
                        <%= stats.teachers %>
                    </div>
                    <div class="stats-label">
                        <i class="fas fa-chalkboard-teacher me-2"></i>
                        Teachers
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-number">
                        <%= stats.quizzes %>
                    </div>
                    <div class="stats-label">
                        <i class="fas fa-question-circle me-2"></i>
                        Quizzes
                    </div>
                </div>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <div class="stats-card">
                    <div class="stats-number">
                        <%= stats.content %>
                    </div>
                    <div class="stats-label">
                        <i class="fas fa-file-alt me-2"></i>
                        Study Materials
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <!-- Usage & Limits -->
            <div class="col-lg-6">
                <div class="progress-card">
                    <h5 class="mb-3">
                        <i class="fas fa-chart-bar text-primary me-2"></i>
                        Plan Usage
                    </h5>
                    
                    <!-- Students Usage -->
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-2">
                            <span>Students</span>
                            <span>
                                <%= stats.students %>
                                <% if (limits.maxStudents !== -1) { %>
                                    / <%= limits.maxStudents %>
                                <% } else { %>
                                    / Unlimited
                                <% } %>
                            </span>
                        </div>
                        <div class="usage-bar">
                            <div class="usage-fill" style="width: <%= limits.maxStudents === -1 ? '20' : Math.min((stats.students / limits.maxStudents) * 100, 100) %>%"></div>
                        </div>
                    </div>
                    
                    <!-- Quizzes Usage -->
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-2">
                            <span>Quizzes</span>
                            <span>
                                <%= stats.quizzes %>
                                <% if (limits.maxQuizzes !== -1) { %>
                                    / <%= limits.maxQuizzes %>
                                <% } else { %>
                                    / Unlimited
                                <% } %>
                            </span>
                        </div>
                        <div class="usage-bar">
                            <div class="usage-fill" style="width: <%= limits.maxQuizzes === -1 ? '20' : Math.min((stats.quizzes / limits.maxQuizzes) * 100, 100) %>%"></div>
                        </div>
                    </div>
                    
                    <!-- Storage Usage -->
                    <div class="mb-3">
                        <div class="d-flex justify-content-between mb-2">
                            <span>Storage</span>
                            <span>
                                <%= Math.round(organization.stats.storageUsed) %> MB
                                <% if (limits.maxStorage !== -1) { %>
                                    / <%= limits.maxStorage %> MB
                                <% } else { %>
                                    / Unlimited
                                <% } %>
                            </span>
                        </div>
                        <div class="usage-bar">
                            <div class="usage-fill" style="width: <%= limits.maxStorage === -1 ? '20' : Math.min((organization.stats.storageUsed / limits.maxStorage) * 100, 100) %>%"></div>
                        </div>
                    </div>
                    
                    <% if (organization.planType === 'free') { %>
                    <div class="text-center mt-3">
                        <a href="/organization/billing" class="btn btn-primary">
                            <i class="fas fa-arrow-up me-2"></i>Upgrade Plan
                        </a>
                    </div>
                    <% } %>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="col-lg-6">
                <div class="progress-card">
                    <h5 class="mb-3">
                        <i class="fas fa-bolt text-warning me-2"></i>
                        Quick Actions
                    </h5>
                    
                    <a href="/organization/invite-student" class="quick-action">
                        <i class="fas fa-user-plus text-primary mb-2" style="font-size: 1.5rem;"></i>
                        <div>Invite Students</div>
                        <small class="text-muted">Add students to your organization</small>
                    </a>
                    
                    <a href="/create-quiz" class="quick-action">
                        <i class="fas fa-plus-circle text-success mb-2" style="font-size: 1.5rem;"></i>
                        <div>Create Quiz</div>
                        <small class="text-muted">Build a new quiz for your students</small>
                    </a>
                    
                    <a href="/teacher/post-content" class="quick-action">
                        <i class="fas fa-upload text-info mb-2" style="font-size: 1.5rem;"></i>
                        <div>Upload Content</div>
                        <small class="text-muted">Share study materials</small>
                    </a>
                    
                    <a href="/teacher/student-results" class="quick-action">
                        <i class="fas fa-chart-line text-warning mb-2" style="font-size: 1.5rem;"></i>
                        <div>View Analytics</div>
                        <small class="text-muted">Track student performance</small>
                    </a>
                </div>
            </div>
        </div>

        <!-- Recent Activity -->
        <div class="row">
            <div class="col-12">
                <div class="recent-activity">
                    <h5 class="mb-3">
                        <i class="fas fa-clock text-primary me-2"></i>
                        Recent Quiz Results
                    </h5>
                    
                    <% if (recentResults && recentResults.length > 0) { %>
                        <% recentResults.forEach(result => { %>
                        <div class="activity-item">
                            <div class="row align-items-center">
                                <div class="col-md-4">
                                    <strong><%= result.student.displayName %></strong>
                                    <div class="text-muted small"><%= result.student.email %></div>
                                </div>
                                <div class="col-md-4">
                                    <span class="badge bg-primary"><%= result.quiz.title %></span>
                                </div>
                                <div class="col-md-2">
                                    <span class="badge <%= result.percentage >= 80 ? 'bg-success' : result.percentage >= 60 ? 'bg-warning' : 'bg-danger' %>">
                                        <%= result.percentage %>%
                                    </span>
                                </div>
                                <div class="col-md-2 text-muted small">
                                    <%= new Date(result.createdAt).toLocaleDateString() %>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                    <% } else { %>
                        <div class="text-center text-muted py-4">
                            <i class="fas fa-inbox fa-3x mb-3"></i>
                            <h6>No quiz results yet</h6>
                            <p>Results will appear here once students start taking quizzes.</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Features Available -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="progress-card">
                    <h5 class="mb-3">
                        <i class="fas fa-star text-warning me-2"></i>
                        Available Features
                    </h5>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-<%= limits.features.multiLanguage ? 'check text-success' : 'times text-muted' %> me-2"></i>
                                <span class="<%= limits.features.multiLanguage ? '' : 'text-muted' %>">Multi-language Support</span>
                            </div>
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-<%= limits.features.advancedAnalytics ? 'check text-success' : 'times text-muted' %> me-2"></i>
                                <span class="<%= limits.features.advancedAnalytics ? '' : 'text-muted' %>">Advanced Analytics</span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-<%= limits.features.customBranding ? 'check text-success' : 'times text-muted' %> me-2"></i>
                                <span class="<%= limits.features.customBranding ? '' : 'text-muted' %>">Custom Branding</span>
                            </div>
                            <div class="d-flex align-items-center mb-2">
                                <i class="fas fa-<%= limits.features.apiAccess ? 'check text-success' : 'times text-muted' %> me-2"></i>
                                <span class="<%= limits.features.apiAccess ? '' : 'text-muted' %>">API Access</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>